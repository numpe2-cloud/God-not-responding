name: muj prvni workflow
on:
    push:
        branches:
            - master
jobs:
    pozdrav:
        runs-on: ubuntu-latest
        steps:
            - name: vypis text
              run: echo "Ahoj z github actions!"
